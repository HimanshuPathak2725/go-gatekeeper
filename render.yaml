services:
  - type: web
    name: gatekeeper-relay
    env: go
    region: oregon
    plan: free
    buildCommand: go build -ldflags="-s -w" -o gatekeeper-server ./cmd/server
    startCommand: ./gatekeeper-server
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
